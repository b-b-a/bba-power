<?php
	$this->title = "BBA Supplier Update";
	$this->headTitle($this->title);
?>

<div class="form_wrap">
    <?php echo $this->supplierSaveForm; ?>
</div>

<div class="clear">&nbsp;</div>

<script>

    var contractStore = <?php echo $this->contractStore; ?>;
    var contactStore = <?php echo $this->contactStore; ?>;
    dojo.addOnLoad(function(){
        dojo.connect(contractGrid, "onRowClick", function(val){
            selectedIndex = contractGrid.focus.rowIndex;
            selectedItem = contractGrid.getItem(selectedIndex);
            //Not sure if this is the most efficient way but it worked for me
            selectedId = contractGrid.store.getValue(selectedItem, "contract_idContract");

            location.href = '/contract/edit/contractId/' + selectedId;
        });
    });

    dojo.addOnLoad(function(){
        dojo.connect(contactGrid, "onRowClick", function(val){
            selectedIndex = contactGrid.focus.rowIndex;
            selectedItem = contactGrid.getItem(selectedIndex);
            //Not sure if this is the most efficient way but it worked for me
            selectedId = contactGrid.store.getValue(selectedItem, "supplierCo_idSupplierContact");

            location.href = '/supplier-contact/edit/contactId/' + selectedId;
        });
    });

</script>

<?php echo $this->partial('_table-head.phtml', array(
    'heading'   => 'Contracts',
    'label'     => 'New Contract',
    'location'  => $this->url(array(
        'controller'    => 'contract',
        'action'        => 'add',
        'supplierId'    => $this->supplier->idSupplier
    ),'default', true)
)); ?>

<div dojotype="dojo.data.ItemFileReadStore" jsid="contractTableStore" data="contractStore"></div>

<table dojoType="dojox.grid.DataGrid"
       jsId="contractGrid"
       store="contractTableStore"
       query="{ contract_idContract: '*' }"
       style="width: 100%;"
       autowidth="false"
       autoHeight="true"
       rowSelector="0px"
       rowsPerPage="25"
       noDataMessage="<span class='dojoxGridNoData'>This Supplier has no contracts yet.</span>">
    <thead>
        <tr>
            <th field="contract_idContract" width="75">Id</th>
            <th field="contract_type" width="150">Type</th>
            <th field="contract_status" width="100">Status</th>
            <th field="contract_dateEnd" width="100">End Date</th>
            <th field="contract_reference" width="100">Reference</th>
            <th field="" width="auto"></th>
        </tr>
    </thead>
</table>

<?php echo $this->partial('_table-head.phtml', array(
    'heading'   => 'Contacts',
    'label'     => 'New Contact',
    'location'  => $this->url(array(
        'controller'    => 'supplier-contact',
        'action'        => 'add'
    ),'default', true)
)); ?>

<div dojotype="dojo.data.ItemFileReadStore" jsid="contactTableStore" data="contactStore"></div>

<table dojoType="dojox.grid.DataGrid"
       jsId="contactGrid"
       store="contactTableStore"
       query="{ supplierCo_idSupplierContact: '*' }"
       style="width: 100%;"
       autowidth="false"
       autoHeight="true"
       rowSelector="0px"
       rowsPerPage="25"
       noDataMessage="<span class='dojoxGridNoData'>This supplier has no contacts yet.</span>">
    <thead>
        <tr>
            <th field="supplierCo_idSupplierContact" width="75">Id</th>
            <th field="supplierCo_name" width="150">Name</th>
            <th field="supplierCo_phone" width="100">Phone</th>
            <th field="supplierCo_email" width="300">Email</th>
            <th field="supplierCo_address1" width="200">Address 1</th>
            <th field="supplierCo_postcode" width="auto">Postcode</th>
            <th field="" width="auto"></th>
        </tr>
    </thead>
</table>