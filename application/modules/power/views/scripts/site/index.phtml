<?php
	$this->title = "BBA Sites";
	$this->headTitle($this->title);

    echo $this->siteSearchForm;
    /* @var $site Power_Model_Site */
?>

<p>
    <a href="<?php echo $this->url(array(
        'controller'    => 'site',
        'action'        => 'add'
    ), 'default', true);?>" class="button">Add new Site</a>
</p>

<table>
	<tr>
	<th>Client <span class="id">(id)</span></th>
    <th>Site</th>
	<th>&nbsp;</th>
	</tr>
    <?php foreach($this->sites as $site): ?>
	<tr>
		<td>
            <?php echo $this->escape($site->client);?>&nbsp;
            <span class="id">(<?php echo $this->escape($site->id); ?>)</span>
        </td>
		<td><?php echo $this->formatSiteAddress($this->escape($site->site)); ?></td>
		<td class="action">
			<a href="<?php echo $this->url(array(
                'controller'    => 'site',
                'action'        => 'edit',
                'siteId'        => $site->id
            ));?>" class="button">Edit</a>
			<a href="<?php echo $this->url(array(
                'controller'    =>'site',
                'action'        =>'delete',
                'siteId'        => $site->id
            ));?>" class="button">Delete</a>
		</td>
	</tr>
    <?php endforeach;?>
</table>

<?php
if ($this->sites instanceof Zend_Paginator) {
    echo $this->paginationControl(
            $this->sites,
            'Sliding',
            '_paginate.phtml',
            array('params' => $this->search)
    );
}
?>
