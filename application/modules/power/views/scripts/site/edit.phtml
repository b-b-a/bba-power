<div data-dojo-type="dijit.layout.BorderContainer"
     data-dojo-props="design: 'headline'"
     class="tabContainer">

    <div data-dojo-type="dijit.layout.ContentPane"
         data-dojo-props="region: 'top'">
        <div class="tabHeading">

            <div class="form_wrap zend_form site_edit ident alpha"
                 dojoType="dijit.form.Form"
                 id="edit-site-<?php echo $this->site->id; ?>">
                <h3>Site Details</h3>
                <div class="form_row">
                    <p class="label"><b>Client:</b></p>
                    <p>&nbsp;<?php echo $this->site->client_name; ?></p>
                </div>

                <button dojoType="dijit.form.Button"
                    type="submit"
                    name="edit-site-button-<?php echo $this->site->id; ?>"
                    value="submit">Edit Details</button>
            </div>

            <div class="ident">
                <h3>Address</h3>
                <div>
                    <p><?php
                        $mainContact = $this->siteHelper($this->site->id)
                            ->getParentRow('siteAddress');

                        echo $this->formatAddress(array(
                            $mainContact->get('clientAd_addressName'),
                            $mainContact->get('clientAd_address1'),
                            $mainContact->get('clientAd_address2'),
                            $mainContact->get('clientAd_address3'),
                            $mainContact->get('clientAd_postcode')
                        ));
                    ?></p>
                </div>
            </div>

            <div class="ident">
                <h3>Billing Address</h3>
                <div>
                    <p><?php
                        $mainContact = $this->siteHelper($this->site->id)
                            ->getParentRow('siteAddressBill');

                        echo $this->formatAddress(array(
                            $mainContact->get('clientAd_addressName'),
                            $mainContact->get('clientAd_address1'),
                            $mainContact->get('clientAd_address2'),
                            $mainContact->get('clientAd_address3'),
                            $mainContact->get('clientAd_postcode')
                        ));
                    ?></p>
                </div>
            </div>

            <div class="ident">
                <h3>Contact</h3>
                <div>
                    <p><?php
                        $mainContact = $this->siteHelper($this->site->id)
                            ->getParentRow('siteClientContact');

                        echo $this->formatAddress(array(
                            $mainContact->get('clientCo_name'),
                            $mainContact->get('clientCo_position'),
                            $mainContact->get('clientCo_phone'),
                            $mainContact->get('clientCo_email')
                        ));
                    ?></p>
                </div>
            </div>

            <div class="clear"></div>

            <div class="tableHead">
                <?php if ($this->authInfo('role') == 'admin'): ?>
                <p class="tableHead-button">
                    <button dojoType="dijit.form.Button"
                            id="new-meter-button-<?php echo $this->site->id; ?>"
                            type="button">New Meter</button>
                </p>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <div id="SiteTabs<?php echo $this->site->id; ?>"
         data-dojo-type="dijit.layout.TabContainer"
         data-dojo-props="region: 'center', tabStrip: 'true'"
         class="innerTabs">

        <div data-dojo-type="dijit.layout.ContentPane"
             data-dojo-props="region: 'center', title: 'Meters'">

            <span dojoType="dojox.data.QueryReadStore"
                 jsid="meterStore<?php echo $this->site->id; ?>"
                 url="/meter/site-meter-store"
                 requestMethod="post"></span>

            <table dojoType="bba.DataGrid"
                   jsId="meterGrid<?php echo $this->site->id; ?>"
                   store="meterStore<?php echo $this->site->id; ?>"
                   query="{ meter_idSite: <?php echo $this->site->id; ?> }"
                   queryParent="meter_idSite"
                   sortInfo="-3"
                   tabs="true"
                   tabTitleColumn="meter_numberMain">
                <thead>
                    <tr>
                        <th field="meter_idMeter" width="50">Id</th>
                        <th field="meter_type" width="100">Type</th>
                        <th field="meter_numberSerial" width="200">Number Serial</th>
                        <th field="meter_numberMain" width="200">Number Main</th>
                        <th field="" width="auto"></th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>

