<?php
	$this->title = "BBA Meters";
	$this->headTitle($this->title);

    echo $this->meterSearchForm;
    /* @var $meter Power_Model_Meter */
?>

<table>
	<tr>
	<th>Client</th>
	<th>Site Address1</th>
	<th>&nbsp;</th>

	</tr>
    <?php foreach($this->meter as $meter) : ?>
	<tr>
        <td><?php echo $this->escape($meter->client); ?></td>
        <td><?php echo $this->escape($meter->site); ?></td>
		<td class="action">
			<a href="<?php echo $this->url(array(
                'controller'    => 'reading',
                'action'        => 'index',
				'meterId'       => $meter->id
            ), 'default');?>" class="button">Readings</a>

			<a href="<?php echo $this->url(array(
                'controller'    => 'meter',
                'action'        => 'edit',
				'meterId'       => $meter->id
            ));?>" class="button">Edit</a>

			<a href="<?php echo $this->url(array(
                'controller'    => 'meter',
                'action'        => 'delete',
                'meterId'       => $meter->getId()
            ));?>" class="button">Delete</a>
		</td>
	</tr>
    <?php endforeach; ?>
</table>

<p>
    <a href="<?php echo $this->url(array(
        'controller'    => 'meter',
        'action'        => 'add'
        ));?>" class="button">Add New Meter</a>
</p>